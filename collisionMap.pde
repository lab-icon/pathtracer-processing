import java.util.HashMap;
import processing.data.JSONObject;

HashMap<String, Object>[] lines = new HashMap[16];
String json = JSONObject.parse(jsonString);

void mapper() {
  for (int i = 0; i < lines.length; i++) {
    lines[i] = new HashMap<String, Object>();
  }






}
